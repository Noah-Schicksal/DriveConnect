# Banco de Dados
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/driveconnect?schema=public"

# Configurações de Criptografia (Argon2id)
#Argon2id configura a memória e tempo de permanencia que o servidor usa para validar as senhas 1020 = 1mib
ARGON2_MEMORY_COST=1024 #1MiB
ARGON2_TIME_COST=2
ARGON2_PARALLELISM=1

# InfinitePay
INFINITEPAY_HANDLE=sua_infinite_tag
INFINITEPAY_WEBHOOK_SECRET=sua_chave_secreta_aqui

# Aplicação
APP_URL=http://localhost:3000
FRONTEND_URL=http://localhost:8080
PAGAMENTO_EXPIRACAO_MINUTOS=10

# HTTPS Enforcement
# Em produção: NODE_ENV=production
# Se estiver atrás de um reverse proxy (Nginx, Caddy, Railway, etc.): HTTPS_BEHIND_PROXY=true
# Se o Node.js estiver exposto diretamente com TLS: HTTPS_BEHIND_PROXY=false
NODE_ENV=development
HTTPS_BEHIND_PROXY=true

# Rotas Base (Múltiplas instâncias no mesmo domínio)
# Deixe em branco se a API estiver rodando direto na raiz
# Exemplo: API_BASE_PATH=/server1
API_BASE_PATH=

# Chave API Pública para o frontend acessar imagens via /storage
PUBLIC_API_KEY=driveconnect_public_key_aqui